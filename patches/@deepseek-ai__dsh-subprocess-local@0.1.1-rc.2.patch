diff --git a/lib/index.js b/lib/index.js
--- a/lib/index.js
+++ b/lib/index.js
@@ -1285,5 +1285,9 @@ var LocalSubprocessRuntime = class extends SubprocessRuntime {
 		});
 		handle.done.then(release, release);
+		// Observability only: readers are cursor-free; no environment/argv is exposed.
+		try {
+			this.ctx.emit("subprocess/spawn", handle, { cwd: spec.cwd, signal: spec.signal });
+		} catch (_subprocessObserverFailed) {}
 		return handle;
 	}
 	async spawnTerminal(spec) {
