# Changelog

## 未发布

- 增加连接现有 ColdX Host 的终端界面开发预览，复用原生 DSH 会话、事件、工具和审批；保留 Web 处理富媒体与较早历史。
- 工作台默认收起任务摘要，统一文件面板与对话区的宽度，并把主视图和已打开文件分成两行，避免中等窗口下内容被挤窄、标签被裁切。
- 将键盘、触屏和高对比度样式纳入实际客户端构建，与浏览器测试使用同一套规则。

## 0.2.0

- 新增 ColdX 请求调度、上下文计量和数值运行诊断；保留 DSH 会话、模型、工具与插件基础设施。
- 重写流式协议扫描热路径，保持围栏示例、原生调用及最终文本语义；附带输出等价的基准脚本。
- 修复执行层关闭时的请求、工具及停止后追问竞态，保留等待中的用户消息。
- 工作区统一为紧凑任务首页、中央对话、右侧结果布局；运行详情按需读取，保留深浅主题与窄屏抽屉。
- 工作面板单次投影共享历史扫描，避免跨渲染缓存可变会话造成过时结果。

实际测量及边界见 [执行层与工作区技术记录](docs/kernel-workspace-2026-09-19.md)。

## 0.1.2

修复独立安装版调用浏览器时提示“浏览器组件尚未安装”的发行缺口。

- 桌面安装包携带匹配 Playwright 版本的 Chromium headless shell，浏览器工具无需首次联网下载或运行 pnpm 安装命令。
- 检测和启动使用同一个 headless shell；完整 Chrome 的存在不再被误当作该组件可用。
- 内置浏览器目录从桌面后端显式传到 MCP 子进程，不依赖开发机或用户的全局 Playwright 缓存。
- 发布检查覆盖浏览器组件缺失与路径边界，并以隔离的本地网页测试已打包驱动的点击和截图。

## 0.1.1

ColdX 首次发布，基于 DeepSeek Harness 0.1.1-rc.2。

### 桌面应用

- Windows x64 独立安装包，内置 Node、pnpm 与锁定的原生运行时。
- Windows/Linux 使用约 40px 一体标题栏，包含导航与菜单，跟随 ColdX 深浅主题；保留原生窗口按钮和 F11 全屏。
- 应用资料与安装目录分离；Windows 长路径安装和卸载保留数据边界。
- Windows x64 完成实际安装、启动、退出与卸载检查；其他平台尚未实机验收。

### 工作台

- 加号菜单统一承载文件、图片、Goal、Plan 和插件入口；模式标签采用一致样式。
- 紧凑的模型与推理强度控件，保留拖动反馈、键盘操作和最高档位动效。
- 新 Logo、统一组件样式，以及深浅主题和减少动态效果支持。
- 对话内生成交互页面，通过原生工具等待提交选择并继续任务。
- 工作面板统一呈现进度、成果和文件预览；支持产物链接、文件引用与 PDF 翻页/缩放/文本选取。
- 子智能体使用原生任务状态和路由，支持紧凑进度及所属文件、终端查看。
- 可选终端面板展示原生命令的实时输出与结束状态；不是交互式 PTY。

### 插件与运行时

- GitHub dsh-plugin 社区目录、详情、发布包核验和一键安装。
- AI 可按需搜索、检查和安装插件，遵循会话权限与用户开关。
- 手动操作和 AI 共用真实安装记录、失败重试、配置/重启提示与持久化恢复。
- 保留 DSH 原生模型、会话、权限和插件生命周期；提供稳定提示词、按需上下文和缓存用量展示。

本版本的功能说明及使用方式见 [README](README.md)。安装附件与后续版本见 [Releases](https://github.com/DCM-dc/ColdX/releases)。研究文档包含提案和历史验收记录，不作为未实现功能或其他平台已验证的承诺。
