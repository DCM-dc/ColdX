Blender: observe the active editor, object selection and mode before an action. Keyboard shortcuts depend on the editor under the pointer and Object/Edit/Pose mode. Focus the intended editor; use its menus or known shortcuts only after observing the current context. Confirm transformed geometry or changed fields in a new screenshot. UI accessibility may be sparse, so use image coordinates and small reversible steps. Verify save/export dialogs, directory and file name. Do not overwrite an existing project unless authorized. A click or key return is not proof of a successful render; inspect the render status and produced image/file.
