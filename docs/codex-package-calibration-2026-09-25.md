# Codex 安装包检查与 ColdX 界面校准

日期：2026-09-25。依据用户“直接拆包”的要求，读取本机安装版本 **26.915.4065.0** 的 `app/resources/app.asar`，没有修改 Codex 安装目录。

## 本地检查材料

索引包含 15,584 个路径，选择提取了 135 个界面相关资源。清单、原始文件及 SHA-256 保留在 Git 忽略目录 `.runtime/codex-package-26.915.4065/`，入口是 `inventory.json` 和 `manifest.json`。ColdX 构建不读取该目录；没有把 Codex 的可执行代码、字体或图标加入产品。

主要依据：

| 资源 | SHA-256 |
| --- | --- |
| `app-shared-11c21cbb0024.css` | `12da57c84d20bee2c427516d7864eed776a869b21ab375083f9da112a8c1d630` |
| `app-initial-19d25b9d212e.css` | `a4430b681135f131cc123806f480086b387e33b3f716ee83f578c67972523290` |
| `thread-scroll-layout-6d6460740166.css` | `810844dff4b38efbc5a74b3508ce8dab5a6da1a077d0dbd2830ac31354976339` |
| `user-message-8b0705662651.css` | `bdae837310efb2de763f5a5e9b176b686a9e14b51b38802800e9c68335cad1f5` |

## 已落到 ColdX 的调整

实现位于 `plugin/client/workbench-rebuild.css` 和 `plugin/client/workbench-entry.mjs`，通过现有客户端构建生成 `client.js`。不改 DSH 的会话、工具执行、权限和输入控制。

| Before | After | Why |
| --- | --- | --- |
| 正文/输入框上限 740px | 768px | 使用包内桌面 `48rem` 定义，并在 ColdX 页面读取实际宽度确认 |
| 输入框圆角 20px、按钮 32px | 圆角 22px、小按钮 28px | 与桌面组件尺寸对应；触屏仍保留 44px 点击区域 |
| 24px 行高、三层输入偏移较大 | 20px 行高、12px 水平内边距 | 输入、镜像和背景投影同步，真实键入与文件引用时高度一致 |
| 底部留白 16px、明显实线边框 | 底部 20px、细阴影描边 | 更接近参考输入框的边界和位置；焦点不改变尺寸 |
| 普通导航 32px、底部设置单独大行 | 31px 导航与设置行 | 按桌面文字行高和上下内边距统一密度 |
| 用户气泡圆角 20px、上限 525px/82% | 16px 圆角、上限 456px | 对照桌面紧凑用户消息布局 |
| 混用旧主题颜色 | 主背景、侧栏、输入框、菜单、气泡、主次文字统一深浅色映射 | 对照安装包基础配色；浮层在深色下使用较高亮度的表面 |
| 模式菜单悬停仍会位移/缩放 | 只改变背景色 | 保持菜单行与指针位置稳定 |

注意：这些是包内 **桌面默认定义**，不是所有运行状态的最终计算值。Codex 还支持自定义对比度、字体、窗口透明效果、缩放和多种组件变体。本轮没有复制 Windows 透明材质，也没有完成所有界面的像素差分；不能据此声称完全像素一致或功能全部对齐。旧截图估算的主区宽度、圆角与按钮尺寸由本报告替代。

## 本轮实际验证

在独立 `.runtime/rebuild-ui-home` 配置和 `.runtime/kernel-ui-work` 工作区运行 `http://127.0.0.1:3087/`，未更改正式聊天数据。本轮交互没有提交收费模型请求。

- 页面计算尺寸：输入框 768×96px、圆角 22px；加号 28px；导航 31px；用户气泡圆角 16px。
- 真键盘输入中文、英文和换行；输入/镜像/背景三层行高、内边距和高度相同。
- 同时打开 Goal/Plan，两个标签均为 28px 高，没有新增可见空消息；测试结束已关闭标签并清空测试草稿。
- 深浅色切换，正文、侧栏、气泡、输入框与文件预览同步变化。测试结束恢复浅色。
- 文件链接打开侧栏，引用文件进入原生输入框；并排时输入区缩窄仍可操作，没有页面水平溢出。
- 900×700 时自动使用 460px 文件抽屉和原生 modal 焦点约束，Escape 关闭并返回触发位置。已重置临时视口。
- 最后读取页面错误日志为空。
- `pnpm test`：729 项，727 通过、2 项环境跳过、0 失败，日志 `.runtime/package-calibration-full-tests.log`。
- 浮层配色最后调整后再次运行原生模型菜单浏览器测试：1/1 通过，包含滑动、键盘、保存中状态与失败恢复；日志 `.runtime/package-calibration-model-browser.log`。
- 客户端构建成功；`git diff --check` 通过。

本轮没有制作或安装新的桌面安装包，未推送未整理的工作区改动。先前验收报告列出的功能边界仍然适用。
